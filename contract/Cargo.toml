[package]
name = "drc20"
version.workspace = true
edition.workspace = true
license.workspace = true

[target.'cfg(target_family = "wasm")'.dependencies]
dusk-core = { workspace = true, features = ["abi-dlmalloc"] }
drc20-types = { path = "../types" }

[lib]
crate-type = ["cdylib"]
